/*
* Work out the number of seconds in a year.
*/

var secondsInMinute = 0;
var minutesInHour = 0;
var hoursInDay = 0;
var daysInYear = 0; // 2016 actually has 366!

// how can we use the variables we declared, in our above code, to work out
// the answer to our problem?
var secondsInYear = 0;

/*
* Work out how many slices of pizza everyone can have at the end of class!
*/

var numberOfPizzas = 0;
var numberOfSlicesPerPizza = 8;
var numberOfPeople = 0;

// how can we use the variables we declared, in our above code, to work out
// the answer to our problem?
var slicesPerPerson = 0;